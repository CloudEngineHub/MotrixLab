# @package _global_
# Task dm-hopper-stand: skrl.ppo, train_backend=jax delta.
defaults:
  - /task/dm-hopper-stand/skrl.ppo@_global_
  - _self_
task:
  train_backend: jax
algo:
  agent:
    learning_epochs: 4
    mini_batches: 4
