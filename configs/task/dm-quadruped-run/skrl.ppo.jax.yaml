# @package _global_
# Task dm-quadruped-run: skrl.ppo, train_backend=jax delta.
defaults:
  - /task/dm-quadruped-run/skrl.ppo@_global_
  - _self_
task:
  train_backend: jax
algo:
  trainer:
    timesteps: 20000
