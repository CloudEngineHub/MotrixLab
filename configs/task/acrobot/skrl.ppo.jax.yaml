# @package _global_
# Task acrobot: skrl.ppo, train_backend=jax delta.
defaults:
  - /task/acrobot/skrl.ppo@_global_
  - _self_
task:
  train_backend: jax
algo:
  agent:
    entropy_loss_scale: 0.005
