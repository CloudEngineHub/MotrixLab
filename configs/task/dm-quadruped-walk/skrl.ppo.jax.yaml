# @package _global_
# Task dm-quadruped-walk: skrl.ppo, train_backend=jax delta.
defaults:
  - /task/dm-quadruped-walk/skrl.ppo@_global_
  - _self_
task:
  train_backend: jax
algo:
  trainer:
    timesteps: 20000
