defaults:
  - export_onnx_schema
  - _self_
  - override hydra/job_logging: stdout

hydra:
  output_subdir: null
  run:
    dir: .
  job:
    chdir: false

run_dir: ???
output: null
opset: 18
parity:
  seed: 1
  samples: 32
  atol: 1e-4
  rtol: 1e-5
