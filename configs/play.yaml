defaults:
  - play_schema
  - _self_
  # Console logging only; don't drop a <job>.log file in the working dir.
  - override hydra/job_logging: stdout

# Overrides the simulator name recorded in the run metadata for manager envs.
sim: null

hydra:
  output_subdir: null
  run:
    dir: .
  sweep:
    dir: .
    subdir: ${hydra.job.num}
  job:
    chdir: false
