defaults:
  - _self_
  - override hydra/job_logging: stdout

hydra:
  output_subdir: null
  run:
    dir: .
  sweep:
    dir: .
    subdir: ${hydra.job.num}
  job:
    chdir: false

artifact: ???

rollout:
  steps: null

  duration_s: 300.0
# Shared policy-control gains for physical deployment recipes.
backend:
  kp: 50
  kd: 1

seed: 1
hardware:
  confirm: false
realtime: true
viewer: false
